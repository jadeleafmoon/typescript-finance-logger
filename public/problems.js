"use strict";
// variables
let age;
age = 10;
// arrays
let arr = [];
arr.push(10);
let names;
names = [];
names.push("bob");
// objects
let ninja;
ninja = {
    age: 10,
    belt: "black",
    skills: ["punch", "kick"]
};
ninja["skills"].push("jump");
const myAdd = (a, b) => {
    let result = "zero";
    result = `${a} ${b}`;
    return result;
};
